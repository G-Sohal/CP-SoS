#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <map>
#include <set>
#include <string>
#define ll long long
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll t; cin>>t;
    while(t--) {
        int n, m; cin>>n>>m;
        vector<int> a(n, 0);
        for(int i=0; i<n; i++) cin>>a[i];
        int k; cin>>k;
        vector<int> b(k, 0);
        for(int i=0; i<k; i++) cin>>b[i];
        
        
    }
}